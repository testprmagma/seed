do

function run(msg, matches)
  return 'Telebot '.. VERSION .. [[ 
  default source by seed and edit by Unkownhacker
  Admins:
 @Unkownhacker
end
return {
    patterns = {
    "^[!/]([Vv]ersion)$"
  }, 
  run = run 
}
end
